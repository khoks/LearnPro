# RAW_VISION.md — verbatim user brain-dump

> **Source-of-truth note:** This file is the user's **original, unedited input** describing what LearnPro is. Nothing in this file is paraphrased, summarized, or "improved." If a future doc disagrees with what's here, this file wins on *intent*. Any evolution of scope is recorded as **addenda** at the bottom (also verbatim), never by editing the original.

---

## Original message — 2026-04-25

OK so this is a new app which I want to create for anyone who wants to learn coding in software engineering. What this app will do is that it will host containers for the user so that the user can write the code and test and see the output of the code. This app will adapt and continuously learn how about the user who is using it Basically it will Gage what is the level of coding of the user and what is their learning pace and what is their IQ or sharpness when it comes to learning coding and it will propose next steps for the user the best next steps for the user which will help the user learn coding step by step. It will have short term mid term and long term plans created for the user for example short term would be for the current session mid term would be for the day and long term would be for a week and then ultra log plan ultra long term plan for ultimately helping the user master the coding skills. It will present problems to the customer in order for them to learn concepts and it will keep track of the concepts which the user has learned properly. When it presents problems to the customer it will have a system of figuring out whether the user is able to arrive at the solution of the problem in appropriate amount of time, how much time are they taking, how many mistakes are they making, And based on that what else do they need to learn or do they need to learn the same concept again in a different way or in much more detail or in a simpler detail. It will continuously listen to the customer or sorry the user and the user can keep on talking while using the app or they can keep silent as well but it will help if the user speaks so that the app can listen as if the app is actually that mimicking the real life teacher who is listening to their student. This app can not only be used for learning coding but also can be used to learn how to create an LLM model from scratch and what is deep learning and what is what are neural networks and what is machine learning etc so there can be these broad 2 3 or 4 categories of learning paths. In the coding learning path the user can choose the programming language of their choice or the app can also interrogate the user that what is going to be their line of work in future and based on that which 12 or three programming languages they should learn and which one they should master completely and which one they should have operational knowledge on And based on that also the app will also guide the user in learning coding. So the app will have a proper tracking timeline and all the concepts topics problems major milestones which have been crossed by the user and which ones are pending and which ones are required for the user which ones are P1 which ones are P-2 which ones are P-3 for the user. So basically this app has to be very intelligent in terms of how it guides the user to learn for example coding. It will also take of the fact that the user has to be in practice of the problems this app has to connect to probably their Whatsapp or their mobile in such a way that it can give the user notifications about the learning or practice session which they are missing out for that day or the learning and practice session which they have scheduled for the day and which they should not miss otherwise they will lag behind in their learning process because when we don't revise something and when we don't continue our learning we often break routine and we start losing track of our focus we lose discipline we end up in a situation where we don't end up pursuing the learning and we also end up in a situation where we start losing the knowledge and skills which we have acquired so keeping momentum is also very important so this kind of intelligence and notification feature which should also be built in this app. This app will be primarily hosted on laptop just like an open source self hosted project initially but my goal is to make this into a Saas Saas product eventually which will be hosted online and it will have authentication authorization it will have login it will have subscription plans it will have free tiers and paid tiers and etcetera but that is for future but right now we should focus on making it as a self hosted open source project. This app needs to be compatible for Windows and Mac and Linux all three But we can start designing this app initially to target Mac primarily not Mac but Windows primarily and then we can when we are building it for Windows we can keep in mind that it has to in future work for Mac or Linux operating systems as well so and anything which we do in it we should have maybe to do's and stubs to make sure and we should have characters and adapters etc to make sure that in future its capabilities can be expanded to Mac or Linux operating system. And for that matter it should also be compatible for running on iOS iPads or Android iPads and Android phones in future. So its interface should be designed as a reactive interface and it should be usable on a phone format or a laptop format or a pad format. On android and iOS it should be as an app and laptop it should be over a website or in the browser. So initially we are planning to build this app for laptop which will be accessible via browser. This app should be capable of supporting a lot of programming languages like Go Rust Kotlin Java C Python It should also be able to support front end programming languages like Typescript and it should also help the user learn programming frameworks like Spring Boot Hibernate Angular React etc full Basically I imagine that this will leverage knowledge of a 3rd party proprietary LLM just like Open AI Chatgpt 5.5 or Cloud Opus 4.7 behind the scenes. But then it will have its own agentic harness and it will have its own database its own rag or other AI native memory stores which it will use to build the profile of the user to build the progress profile to build the learning path the schedules etc To also curate the next best step for the user and to understand what the user has done on the screen on sandbox environment the sandbox canvas on the UI so that it can figure out what how to respond to the user and what should be shown next to the user It should have a game engine a gamified way of teaching the coding or other concepts to the user. It should tell the user that what is their progress in terms of how many categories and are they 20% in data structures category or are they 40% in building composable UI widgets in react or whatever so that it should maintain a lot of categories across which it should award badges and medals and it should track score for the user. But ultimately it should know that what needs to be told to the user and what next step what next challenge or what next concept the user should learn and the way the user should learn it should be in bite size portions and Via examples via practice problems via different levels of problems in terms of difficulty et cetera. This app can this app should keep track of all the questions challenges examples concepts presented to the user so far and which ones the user understands and which ones the user has escaped and which ones the user has mastered etc. The ultimate goal of this app is to become a personal companion of the user which help the user learn for example cod and help the user keep track of where they are in the coding world and what can they achieve with their skills at present with the amount of things with the number of skills which they have acquired in the amount of knowledge which they have gathered in for example and to let the user know that what they are missing out right now and what they can't do as well. The whole design of the app would be to engage the user as much as possible so that they can increase their skills and knowledge. The users different users of this app should definitely feel as if it is presenting a different plan and adapting the plan and responses according to the user as much as possible so it needs to have a deep understanding of what the user is in several different ways so that its agentic hardness can become personalized contextualized proactive reactive adaptive customized for the user. Now initialize this folder learn pro with git and initialize it with Claude MD and all the other stuff required and save this input which I just gave you as raw input somewhere within this folder for reference Umm as a dump But now I want you to basically dig deep into what I just told you and figure out what features I'm going to be there in this app and let's go into details of those features and groom them and let's see if so there is something else which is also required to make this app really successful for the users.

---

## Addendum 1 — clarifying answers (2026-04-25)

In response to clarifying questions during planning, the user locked in:

- **MVP languages:** Python + TypeScript
- **LLM provider:** Anthropic Claude (primary, behind a provider-abstraction layer)
- **Voice:** Deferred to v1 (text-only MVP)
- **License:** BSL 1.1 (source-available, free for self-host, protects future SaaS; converts to Apache 2.0 on 2030-04-25)

---

## Addendum 2 — in-repo tracking requirement (2026-04-25)

> OK so also make sure that in this repo zittery We store this plan which you have come up with and we break it down into milestones no sorry not milestones like you know classic JIRA structure like features apex JIRA stories and tasks within Zero Stories and then we track it based on that and what we have done and what we have not done instead of justice relying on the session conversation history to figure out what our plan was and what is pending. This plan which we will maintain in the repository itself it will keep on getting updated with new scope and changed scope as per our future discussions going forward as well as we evolve the product.

This led to the creation of `project/` — a JIRA-style Epic / Story / Task hierarchy stored as markdown files in the repo, which is now the operational source of truth for what's done and pending.
